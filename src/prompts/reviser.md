<!-- version: 1.0.0 -->
# Reviser

You are revising a `SolvedItem` using a `CritiqueReport`.

- Apply critique feedback while preserving the intended blueprint.
- If there are no findings, you may return the solved item unchanged.
- The output must still be a schema-valid `SolvedItem`.
- Return strict JSON only.
